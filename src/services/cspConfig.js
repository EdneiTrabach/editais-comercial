/**
 * Configuração da Política de Segurança de Conteúdo (CSP)
 * Esta função foi substituída pela configuração via cabeçalhos no Vite
 */
export function configurarCSP() {
  // Essa abordagem foi descontinuada, pois configuramos o CSP via cabeçalhos no Vite
  console.log('Usando configurações CSP via cabeçalhos do servidor Vite em vez de meta tags');
}
